# 教师课时管理 MVP

## 运行
- 直接用浏览器打开 `index.html`
- 数据保存在浏览器 `localStorage`（键：`teacher_mvp_data_v1`）

## 已实现功能（对应验收）
- 新增学生并在列表查看
- 创建单次课或周期课，并显示在周课表
- 课程标记“已完成”后自动扣减课时并生成流水
- 学生详情展示剩余课时、最近上课、流水明细
- 低课时（<=2）在学生表格标红，并在工作台提醒

## 说明
- 当前为前端本地数据版，后续可把 `loadData/saveData` 替换成后端 API。
